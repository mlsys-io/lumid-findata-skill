id: findata.news.stats
version: 0.1.0
summary: Per-category news row counts (last 7 d / 30 d) + latest-known timestamp.
auth: lumid-pat
impl: impl.py:call
inputs: {}
outputs:
  type: object
  description: |
    `{categories: [{category, rows_last_7d, rows_last_30d, latest_in_60d}]}`.
    Useful for ops dashboards and freshness checks.
